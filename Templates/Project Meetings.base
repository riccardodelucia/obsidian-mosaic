filters:
  and:
    - '!file.path.contains("Templates")'
    - entity == link("Meetings.base", "Meeting")
    - projects.contains(this.file)
views:
  - type: table
    name: Table
    order:
      - file.name
      - participants
      - start
      - end
      - location
